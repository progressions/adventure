.row
  .span12
    %h1 Saved Games

    .form-horizontal
      = form_for(@saved_game) do |f|
        .control-group
          = f.label :name, class: "control-label"
          .controls
            = f.text_field :name

        .form-actions
          = f.submit(class: "btn btn-success")


